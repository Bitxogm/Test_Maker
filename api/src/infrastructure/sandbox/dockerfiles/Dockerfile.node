FROM node:20-alpine
RUN mkdir -p /sandbox /deps && chown -R node:node /sandbox /deps
WORKDIR /deps
USER node
RUN yarn init -y && yarn add vitest@1.6.0 --dev
WORKDIR /sandbox
ENV NODE_PATH=/deps/node_modules
ENV NODE_ENV=test
CMD ["sh"]
